import EventsMedia from "@/pages/news/EventsMedia";

export const metadata = {
    title: "Events & Media | Ayushi Crop Science",
    description: "Gallery of our events, exhibitions, and media coverage.",
};

export default function EventsMediaPage() {
    return <EventsMedia />;
}
